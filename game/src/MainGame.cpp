#include "MainGame.h"

int main(int argc, char* argv[])
{
	InitWindow(screenWidth, screenHeight, gameWindowTitle);	// Initialise the game window
	InitAudioDevice();										// Initialize audio device

}
